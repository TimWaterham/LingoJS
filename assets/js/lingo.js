var randomWord = words[Math.floor(Math.random() * words.length)]; //get Random Word

var wordArray = randomWord.split("", 5); //Split Random Word for checking

this.check = function(id) {
	let fields = [ id + ".1", id + ".2", id + ".3", id + ".4", id + ".5"];
}